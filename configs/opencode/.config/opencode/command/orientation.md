---
description: Quick codebase orientation using Context+ and TLDR tools
agent: code_analyzer
---

Run a quick codebase orientation to build context before making changes:

1. Get the project file structure (use extensions filter to reduce noise)
2. Get file headers and symbol names (use depth_limit: 2 for large projects)
3. Identify architectural layers and circular dependencies
4. Find code by intent using semantic search

Principle: Gather context before editing. Prefer targeted queries once you know what you're looking for. $ARGUMENTS
